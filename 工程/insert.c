
#include<vector>
#include<iostream>
using namespace std;
extern  struct Location{  //存放字符位置的数组
    int rowplace;//字符在行链表的位置；
    int heapplace;//字符在堆的位置；
    int length;//字符长度 
}Location;
void insert(struct Location loca)
{
	;
}
void del(struct Location loca)//删除
{
        ;
}
bool search(vector<char>string)//查找string
{
      ;
}//????建议这里返回值为bool型，找不到？？？ 
